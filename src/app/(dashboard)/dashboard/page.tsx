/**
 * Overview page — temporary stub for Task 1 commit. Task 2 replaces this with
 * the full 5-card grid + empty state + Dashboard-Overview.html parity.
 */
export default function OverviewPagePlaceholder() {
  return (
    <div className="py-stack-md">
      <h1 className="text-2xl font-semibold text-on-surface">Overview</h1>
      <p className="mt-2 text-sm text-on-surface-variant">
        Loading dashboard layout… (overview content lands in next commit).
      </p>
    </div>
  );
}
